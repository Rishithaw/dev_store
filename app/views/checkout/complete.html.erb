<h1>Order Complete</h1>

<% if @last_order %>
  <p>Thanks — your order (<strong>#<%= @last_order.id %></strong>) has been created.</p>

  <h3>Order summary</h3>
  <p>Placed at: <%= @last_order.created_at.strftime("%Y-%m-%d %H:%M") %></p>
  <p>Payment reference: <%= @last_order.payment_reference %></p>

  <%= link_to "Back to products", products_path %>
<% else %>
  <p>No order information available.</p>
  <%= link_to "Back to products", products_path %>
<% end %>
