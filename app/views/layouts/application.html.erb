<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "RW Dev Store" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# Bootstrap CSS (CDN) - using CDN keeps build simple and satisfies framework requirement %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-..." crossorigin="anonymous">

    <%# Your custom styles (SCSS compiled by asset pipeline / Propshaft) %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%= yield :head %>
  </head>

  <body class="bg-off-white text-body">
    <!-- Responsive navbar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom">
      <div class="container">
        <%= link_to "RW Dev Store", root_path, class: "navbar-brand fw-bold text-dark" %>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMenu" aria-controls="navbarMenu" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarMenu">
          <ul class="navbar-nav ms-auto align-items-lg-center">
            <li class="nav-item">
              <%= link_to "Products", products_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "About", about_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Contact", contact_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Cart", cart_path, class: "nav-link position-relative" %>
            </li>

            <% if admin_user_signed_in? %>
              <li class="nav-item ms-2">
                <%= link_to "Admin", admin_root_path, class: "btn btn-outline-secondary btn-sm" %>
              </li>
            <% end %>

            <% if user_signed_in? %>
              <li class="nav-item dropdown ms-2">
                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  Welcome <%= current_user.try(:first_name) || current_user.try(:email) %>
                </a>
                <ul class="dropdown-menu dropdown-menu-end">
                  <li><%= link_to "My Account", edit_user_registration_path, class: "dropdown-item" %></li>
                  <li><%= link_to "Logout", destroy_user_session_path, method: :delete, class: "dropdown-item" %></li>
                </ul>
              </li>
            <% else %>
              <li class="nav-item ms-2">
                <%= link_to "Sign in", new_user_session_path, class: "btn btn-primary btn-sm" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Flash messages -->
    <div class="container mt-4">
      <% flash.each do |k, v| %>
        <div class="alert <%= k == 'notice' ? 'alert-success' : 'alert-danger' %> alert-dismissible fade show" role="alert">
          <%= v %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>
    </div>

    <main class="container my-4">
      <%= yield %>
    </main>

    <footer class="bg-dark text-white py-4 mt-auto">
      <div class="container d-flex justify-content-between">
        <div>
          <strong>RW Dev Store</strong><br>
          Winnipeg, Canada
        </div>
        <div class="text-muted small">© <%= Time.current.year %> RW Dev Store</div>
      </div>
    </footer>

    <!-- Bootstrap JS bundle (CDN) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-..." crossorigin="anonymous"></script>

    <%= javascript_importmap_tags %>
  </body>
</html>
